spring: 
  profiles:  
    active:
    - dev

---
server:
  port: 8001
  
mybatis:
  type-aliases-package: com.efunds.cloud.mapper
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 3000
    
spring:
  profiles: dev
  application:
   name: springcloud-provider-8001
  datasource:
   type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
   driver-class-name: com.mysql.jdbc.Driver                # mysql驱动包
   url: jdbc:mysql://localhost:3306/springcloud1           # 数据库名称
   username: root
   password: 150201
   dbcp2:
     min-idle: 5                                           # 数据库连接池的最小维持连接数
     initial-size: 5                                       # 初始化连接数
     max-total: 5                                          # 最大连接数
     max-wait-millis: 200                                  # 等待连接获取的最大超时时间
     pool-prepared-statements: true
     max-open-prepared-statements: 20
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
  instance:
    instance-id: provider-8001 #注册实例名
    prefer-ip-address: true #超链接显示ip地址

info:
  app.name: springcloud
  company.name: www.baidu.com
  build.artifactId: ^project.artifactId^
  build.version: ^project.version^


---
server:
  port: 8001
  
mybatis:
  type-aliases-package: com.efunds.cloud.mapper
  configuration:
    map-underscore-to-camel-case: true
    default-fetch-size: 100
    default-statement-timeout: 3000
    
spring:
  profiles: test
  application:
   name: springcloud-provider-8001
  datasource:
   type: com.alibaba.druid.pool.DruidDataSource            # 当前数据源操作类型
   driver-class-name: com.mysql.jdbc.Driver                # mysql驱动包
   url: jdbc:mysql://localhost:3306/springcloud2           # 数据库名称
   username: root
   password: 150201
   dbcp2:
     min-idle: 5                                           # 数据库连接池的最小维持连接数
     initial-size: 5                                       # 初始化连接数
     max-total: 5                                          # 最大连接数
     max-wait-millis: 200                                  # 等待连接获取的最大超时时间
     pool-prepared-statements: true
     max-open-prepared-statements: 20
eureka:
  client:
    service-url:
      defaultZone: http://eureka7001.com:7001/eureka/
  instance:
    instance-id: provider-8001 #注册实例名
    prefer-ip-address: true #超链接显示ip地址

info:
  app.name: springcloud
  company.name: www.baidu.com
  build.artifactId: ^project.artifactId^
  build.version: ^project.version^

